# Custom Styling

The React component has been built with custom styling as a first-class function. 

This allows you to  
customize the appearance of the dashboard to match your application's design.

